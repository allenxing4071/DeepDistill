---
description: DeepDistill 项目开发规则
globs:
alwaysApply: true
---

# DeepDistill 项目开发规则

## 🧭 Rules × Skills 协作框架

> Rules 负责"强制边界"，Skills 负责"落地方法"。执行顺序：**先遵守 Rules，再按 Skills 执行**。

### Skill 路由表

> 收到用户指令时，先按此表匹配 Skill，再遵守关联 Rules 执行。

| 用户意图（触发词） | 执行 Skill | 必须遵守的 Rules |
|---|---|---|
| "项目结构/怎么跑/入门" | `.skills/项目入门/SKILL.md` | — |
| "转录/ASR/OCR/文档提取/内容处理" | `.skills/内容处理/SKILL.md` | R0(变更控制) |
| "AI分析/提炼/LLM/prompt/结构化" | `.skills/AI分析/SKILL.md` | R0(变更控制) R5(安全) |
| "视频分析/镜头/场景/风格/动作" | `.skills/视频分析/SKILL.md` | R0(变更控制) |
| "部署/上线/重启/Docker" | `.skills/部署运维/SKILL.md` | R6(部署) R0(变更控制) |
| "UI规范/设计系统/样式/页面视觉/前端" | `.skills/UI/SKILL.md` | R0(变更控制) |
| 修改代码/修 Bug/加功能 | 无专属 Skill，按 Rules 执行 | R0(变更控制) R1(验证) R7(注释) |
| 新经验/踩坑 | 写入对应 Skill 的"经验沉淀"区 | R8(经验沉淀) |

### 原则
- Rules 只保留"必须/禁止/流程边界"，具体方法沉淀到 Skills
- 任何新增经验，必须回写到对应 Skill（见 R8）
- Skill 内的操作仍须遵守 Rules 边界（Skill 不可覆盖 Rules）

---

## 🔴 R0. 变更范围控制（核心代码保护）【最高优先级】

> ⚠️ **此规则优先级最高：未得到用户明确同意，不得改动核心代码；遇到什么问题，只修改什么问题。**

- **默认策略：最小改动**
  - 只修改为解决当前 Bug/需求所必需的代码路径（最小文件数、最小 diff）。
  - 禁止"顺手重构/优化/美化/统一风格/清理无关代码/批量格式化"等非必要改动。

- **核心代码保护：必须先征得用户同意**
  - 任何涉及 **核心链路/核心模块** 的改动，在动手前必须先说明：
    - 具体要改哪些文件/函数
    - 为什么必须改（与当前问题的直接因果）
    - 预期影响面与回归风险
  - 未得到用户明确同意：**不得修改核心代码**，只能做只读分析/提出方案/给出定位结论。

- **核心模块清单**（命中即需授权）：
  - `deepdistill/pipeline.py` — 主管线编排
  - `deepdistill/processing/asr.py` — 语音转文字核心
  - `deepdistill/processing/ocr.py` — 图片 OCR 核心
  - `deepdistill/ai_analysis/llm_client.py` — LLM 调用封装
  - `deepdistill/ai_analysis/prompts/` — 提示词模板
  - `deepdistill/config.py` — 全局配置
  - `deepdistill/video_analysis/` — 视频分析模块

- **禁止越权扩展范围**
  - 不得把"局部问题"上升为"大范围重构/架构调整"。
  - 若发现"根因在别处"需要扩大范围：先停下，向用户解释并等待明确授权后再继续。

- **结果可解释**
  - 每次改动必须能对应到一个明确的问题点，并能用"改了哪里 → 解决了什么 → 如何验证"说明清楚。

---

## 🔴 R1. 功能验证规范【高优先级】

> ⚠️ 每次修改功能或修复 Bug 后必须验证。

### 阶段 A：快速验证
- 使用 CLI 命令验证处理结果是否正确
- 检查输出文件内容、格式、结构是否符合预期
- **此阶段用于快速定位问题，但不能作为最终验证**

### 阶段 B：完整功能验证【必须执行】
- 端到端测试：输入一个真实文件 → 检查完整输出
- 涉及 Web UI 时：浏览器访问 + 截图验证
- 对比预期输出与实际输出的差异

### 验证标准
```
❌ 仅通过代码审查 → 不合格
❌ 仅查看日志 → 不合格
✅ CLI 验证 + 完整输出检查 → 合格
✅ Web UI 相关：接口验证 + 页面截图验证 → 合格
```

---

## 🔴 R2. 管线架构规范【强制】

> ⚠️ DeepDistill 采用分层管线架构，层间通过标准数据结构传递。

### 六层架构

| 层级 | 模块 | 职责 |
|---|---|---|
| Layer 1 | `ingestion/` | 输入层：格式识别与路由 |
| Layer 2 | `processing/` | 内容处理层：ASR / OCR / 文档提取 |
| Layer 3 | `video_analysis/` | 视频增强分析层：镜头/场景/风格/动作 |
| Layer 4 | `ai_analysis/` | AI 分析层：LLM 结构化提炼 |
| Layer 5 | `fusion/` | 融合输出层：去重/合并/格式化 |
| Layer 6 | `knowledge/` | 知识管理层：飞书/Notion/Obsidian |

### 设计原则
- **插件化**：每个处理器实现统一接口，可独立替换
- **本地优先**：默认不调用云端 API，用户显式开启后才使用
- **渐进增强**：MVP 只需 Layer 1-2-4-5，其余按需启用
- **配置驱动**：所有行为通过配置文件控制，代码中不硬编码参数

### 禁止事项
- ❌ **禁止**跨层直接调用（必须经过管线编排）
- ❌ **禁止**在处理器中硬编码模型路径/参数（必须走配置）
- ❌ **禁止**在代码中硬编码 API Key（必须走 `.env`）

---

## 🔴 R3. 模型与资源管理【强制】

> ⚠️ 本项目涉及多个 AI 模型，必须统一管理。

### 模型管理
- 模型文件不入 Git（`.gitignore` 已排除 `models/`、`*.pt`、`*.pth` 等）
- 模型通过配置文件指定路径，支持自动下载
- 大模型首次使用时按需下载，缓存到本地 `~/.cache/deepdistill/`

### GPU/MPS 兼容
- 默认检测可用设备：MPS (Mac) > CUDA > CPU
- 每个模型标注 MPS 兼容性，不兼容的自动 fallback 到 CPU
- 禁止假设用户一定有 GPU

### 内存控制
- 大文件必须分块处理（视频分段、文档分页）
- 处理完一个文件后释放模型内存（可配置是否常驻）
- 批量处理时使用队列，控制并发数

---

## 🔴 R4. 时间与时区统一【强制】

### 统一原则
- **存储/计算**：统一使用 **UTC**（timezone-aware）
- **文件命名/输出**：统一使用 **ISO8601** 格式
- **CLI 展示**：统一展示 **北京时间 `Asia/Shanghai`（UTC+8）**

### 禁止使用 naive datetime
- ❌ `datetime.utcnow()`
- ❌ `datetime.now()`（无 tzinfo）
- ✅ `datetime.now(timezone.utc)`

---

## 🔴 R5. 安全与隐私保护【强制】

- **禁止**在代码/日志/输出中泄露 API 密钥、Token 等敏感信息
- 所有敏感配置**只通过 `.env` 文件**加载，`.env` 已加入 `.gitignore`
- **禁止**将用户内容自动上传到第三方服务（本地优先原则）
- LLM API 调用前必须提示用户：内容将发送到云端
- 日志中涉及文件路径时，脱敏处理（不暴露完整绝对路径）

---

## 🔴 R6. 部署与环境规范【强制】

> 详见 `.skills/部署运维/SKILL.md`

### 本地开发
- Python 3.11+，推荐使用 venv
- 依赖管理：`pyproject.toml` + `pip install -e .`
- 配置：`config/default.yaml` + `.env`

### Docker（可选）
- 提供 `Dockerfile` 和 `docker-compose.yml`
- 数据目录挂载到宿主机，不在容器内存储

### 部署限制
- 未经用户明确授权，**禁止**执行任何部署操作
- 禁止自动执行 `git push`
- 禁止在容器内直接修改代码

---

## R7. 文件用途说明与中文注释【强制】

- 新建文件或修改文件时，文件顶部必须有**"本文件核心用途"**的中文说明（1-3 行）
- 代码注释优先使用中文，保持简洁清晰
- prompt 模板文件使用中文说明用途，提示词本身可用英文

---

## 🔴 R8. 经验沉淀规范【强制】

> ⚠️ 每次任务过程中产出的新经验/新坑位/新最佳实践必须沉淀到对应 Skill。

### 沉淀路由

| 经验类型 | 写入 Skill |
|---|---|
| ASR/Whisper 坑位/精度问题 | `.skills/内容处理/SKILL.md` |
| OCR 识别问题/多语言 | `.skills/内容处理/SKILL.md` |
| LLM 调用优化/prompt 技巧 | `.skills/AI分析/SKILL.md` |
| 视频分析模型/兼容性 | `.skills/视频分析/SKILL.md` |
| 部署/环境/Docker | `.skills/部署运维/SKILL.md` |
| 无明确归属 | `.skills/经验沉淀/SKILL.md` |

### 经验条目模板
```
### 经验：[简要标题]
- 现象:
- 根因:
- 解决:
- 验证:
- 关联: (Rules 编号 / 文件 / 接口)
- 日期:
```

---

## R9. 文档输出限制
- 除非用户明确要求，否则**禁止**自动创建任何 `.md` 文档文件
- 所有说明、解释、方案都应直接在对话中以纯文本形式输出

## R10. Git Push 限制
- 执行 git push 前必须等待用户明确授权
- **禁止**自动执行 push 到任何远程仓库

## R11. 任务执行节奏
- 收到任务后，优先进行**分析和确认**，而非立即执行
- 除非用户明确说"直接做"、"立即执行"、"开始吧"，否则保持询问和等待状态

## 🔴 R12. 方案建议必须基于数据，禁止迎合【强制】

- **禁止迎合**：不得因照顾用户情绪而推荐次优方案
- **数据先行**：涉及模型选择/参数调优时，必须先查找 benchmark 数据再给结论
- **诚实标注不确定性**：当数据不足时，必须说"数据不足，无法确定"

---

## 核心原则

> **Ask First, Act Later** — 先问再做
>
> **本地优先** — 默认不调用云端 API，隐私可控
>
> **插件化** — 每个处理模块可独立替换
>
> **经验必沉淀** — 新坑位/新实践必须写回对应 Skill（R8）
>
> **数据说话，不迎合** — 模型选择/参数建议必须有 benchmark 支撑（R12）

---

## 附录：项目环境速查

### 项目路径
- 本地：`~/Documents/soft/DeepDistill`
- 服务器：`/opt/DeepDistill`（47.254.246.53，与 KKline 共用）

### 域名与访问
- 域名：`deepdistill.kline007.top` → A 记录 `47.254.246.53`
- Web UI：`https://deepdistill.kline007.top`（规划中）
- API：`https://deepdistill.kline007.top/api`（规划中）
- SSH：`ssh -i ~/Documents/soft/KKline/deploy/LH.pem -p 2222 root@47.254.246.53`

### 技术栈
- Python 3.11+
- ASR：ffmpeg + faster-whisper
- OCR：EasyOCR / PaddleOCR
- 文档：PyPDF2 / python-pptx / BeautifulSoup
- 视频分析：PySceneDetect / YOLOv8 / CLIP / MediaPipe
- AI 提炼：DeepSeek / Qwen（本地 + API）
- 输出：Markdown / JSON / Skill 文档

### 常用命令
```bash
cd ~/Documents/soft/DeepDistill

# 安装（开发模式）
pip install -e ".[dev]"

# 处理单个文件
deepdistill process <file>

# 批量处理
deepdistill process <directory>

# 查看配置
deepdistill config show
```
